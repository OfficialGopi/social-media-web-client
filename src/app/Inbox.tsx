import { MessageCircle } from "lucide-react";

const Inbox = () => {
  return (
    <div className="w-full h-full flex items-center justify-center">
      <MessageCircle className="w-60 h-60" />
    </div>
  );
};

export default Inbox;
